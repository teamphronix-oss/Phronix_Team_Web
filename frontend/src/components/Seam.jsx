// The signature "gold seam" divider — a thin trace with a single gold node,
// used between sections instead of a generic border.
export default function Seam() {
  return <div className="seam" aria-hidden="true" />;
}
